[project]
name = "imfuse-infer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch==2.10",
]

[project.optional-dependencies]
mambapy = [
    "mambapy>=1.2.0",
]
mamba = [
    "mamba-ssm>=2.3.1",
]
